apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: node-app-frontend
        image: amandhal/node-app-frontend:1.0.0
        imagePullPolicy: Always
        env:
          - name: BACKEND_URL
            valueFrom:
              configMapKeyRef:
                name: node-app-configmap
                key: BACKEND_URL
        resources:
          requests:
            memory: "32Mi"
            cpu: "50m"
          limits:
            memory: "64Mi"
            cpu: "100m"       
